version: '3.8'

services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: stockmanager-db
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=StockManager@123
    ports:
      - "1433:1433"
    volumes:
      - sqlvolume:/var/opt/mssql

volumes:
  sqlvolume:
